import React from 'react'

import './FancyButton.css'

const FancyButton = (props) => (
    <button className="FancyButton">
        This Button is useless, but fancy.
    </button>
)

export default FancyButton